# Deleted codes

# Fetching the list of files in Data directory
# dataFiles = os.listdir('./Data')

# Removing their .txt extentions to fetch their names
# dataVariations = [dataFile.replace('.txt', '') for dataFile in dataFiles]

# coverage = ', '.join(map(str, datasets[dataset])) ===> get the coverage area of parameters
